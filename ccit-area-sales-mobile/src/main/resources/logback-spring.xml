<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml" />

    <!--彩色日志输出格式-->
    <property name="CONSOLE_LOG_PATTERN" value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(%level){blue} %clr(${PID}){magenta} %clr([%thread]){orange} %clr(%logger){cyan} %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
    <!--非彩色日志输出格式-->
    <property name="PATTERN" value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n" />

    <!--dev文件路径：src同级目录logs,如果上级目录不存在会自动创建-->
    <property name="LOG.PATH" value="/opt/log/"/>

<!--    在logback-spring.xml配置文件读取spring.application.name-->
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue=""/>
    <springProperty scope="context" name="active" source="spring.profiles.active" defaultValue=""/>
    <springProperty scope="context" name="elasticsearch.user" source="elasticsearch.user" defaultValue=""/>
    <springProperty scope="context" name="elasticsearch.password" source="elasticsearch.password" defaultValue=""/>
    <springProperty scope="context" name="elasticsearch.server.host" source="elasticsearch.server.host" defaultValue=""/>
    <springProperty scope="context" name="elasticsearch.server.port" source="elasticsearch.server.port" defaultValue=""/>
    <springProperty scope="context" name="maxHistory" source="log.max-history" defaultValue="30"/>
    <springProperty scope="context" name="logFileName" source="log.filename" defaultValue="nohup-service"/>
    <springProperty scope="context" name="indexName" source="log.indexname" defaultValue="x01-service"/>
    <springProperty scope="context" name="logLevel" source="log.level" defaultValue="INFO"/>

    <!-- 控制台输出 -->
    <appender name="consoleAppender" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- 按照每天生成输出日志文件 ch.qos.logback.core.rolling.RollingFileAppender-->
    <appender name="fileAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 正在记录的日志文档的路径及文档名 -->
        <file>${LOG.PATH}/${appName}/info.log</file>
        <encoder>
            <!--格式化输出：%d表示日期，%thread表示线程，%-5level：级别从左显示五个字符宽度，%logger{36}：logger是class的全名,后面的数字代表限制最长的字符，%msg：日志消息，%n换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset> <!-- 设置字符集 -->
        </encoder>
        <!--滚动策略按照时间滚动-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- rollover daily 文件名称 -->
            <fileNamePattern>${LOG.PATH}/${appName}/info-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- each file should be at most 10MB, keep 60 days worth of history, but at most 2GB -->
            <!--单个文件大小-->
            <maxFileSize>10MB</maxFileSize>
            <!--日志文件保留天数-->
            <maxHistory>${maxHistory}</maxHistory>
            <!--用来指定日志文件的上限大小，到了这个值就会删除旧日志-->
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>

    <!-- 按照每天生成错误日志文件 -->
    <appender name="errorAppender" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- 正在记录的日志文档的路径及文档名 -->
        <file>${LOG.PATH}/${appName}/error.log</file>
        <!-- 此日志文件只记录ERROR级别的 -->
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
            <charset>UTF-8</charset> <!-- 设置字符集 -->
        </encoder>
        <!--输出日志到src同级目录logs中的error.log文件中-->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!--基于大小和时间的轮转策略，当日志内容超出文件大小限制后，会自动生成一个文件来继续记录和重命名-->
            <fileNamePattern>${LOG.PATH}/${appName}/error-%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <!-- each file should be at most 10MB, keep 60 days worth of history, but at most 2GB -->
            <maxFileSize>10MB</maxFileSize>
            <maxHistory>${maxHistory}</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
    </appender>


<!--    <appender name="elasticAppender" class="com.internetitem.logback.elasticsearch.ElasticsearchAppender">-->
<!--&lt;!&ndash;        <url>http://elastic:T02uypuCLW5fAKSia3Dj@10.10.110.121:9200/_bulk</url>&ndash;&gt;-->
<!--        <url>http://${elasticsearch.user}:${elasticsearch.password}@${elasticsearch.server.host}:${elasticsearch.server.port}/_bulk</url>-->
<!--        <index>${indexName}-log-${active}-%d{yyyy-MM-dd}</index>-->
<!--        &lt;!&ndash; 根据启动环境为索引命名,区分各个环境日志 &ndash;&gt;-->
<!--        <type>${active}</type>-->
<!--        <connectTimeout>30000</connectTimeout> &lt;!&ndash; optional (in ms, default 30000) &ndash;&gt;-->
<!--        <errorsToStderr>false</errorsToStderr> &lt;!&ndash; optional (default false) &ndash;&gt;-->
<!--        <includeCallerData>false</includeCallerData> &lt;!&ndash; optional (default false) &ndash;&gt;-->
<!--        <logsToStderr>false</logsToStderr> &lt;!&ndash; optional (default false) &ndash;&gt;-->
<!--        <maxQueueSize>104857600</maxQueueSize> &lt;!&ndash; optional (default 104857600) &ndash;&gt;-->
<!--        <maxRetries>3</maxRetries> &lt;!&ndash; optional (default 3) &ndash;&gt;-->
<!--        <readTimeout>30000</readTimeout> &lt;!&ndash; optional (in ms, default 30000) &ndash;&gt;-->
<!--        <sleepTime>250</sleepTime> &lt;!&ndash; optional (in ms, default 250) &ndash;&gt;-->
<!--        <rawJsonMessage>false</rawJsonMessage> &lt;!&ndash; optional (default false) &ndash;&gt;-->
<!--        <includeMdc>false</includeMdc> &lt;!&ndash; optional (default false) &ndash;&gt;-->
<!--        <maxMessageSize>-1</maxMessageSize> &lt;!&ndash; optional (default -1 &ndash;&gt;-->
<!--        <authentication class="com.internetitem.logback.elasticsearch.config.BasicAuthentication"/> &lt;!&ndash; optional &ndash;&gt;-->
<!--        <properties>-->
<!--            <property>-->
<!--                <name>host</name>-->
<!--                <value>${HOSTNAME}</value>-->
<!--                <allowEmpty>false</allowEmpty>-->
<!--            </property>-->
<!--            <property>-->
<!--                <name>level</name>-->
<!--                <value>%level</value>-->
<!--            </property>-->
<!--            <property>-->
<!--                <name>thread</name>-->
<!--                <value>%thread</value>-->
<!--            </property>-->
<!--            <property>-->
<!--                <name>stacktrace</name>-->
<!--                <value>%ex</value>-->
<!--            </property>-->
<!--            <property>-->
<!--                <name>logger</name>-->
<!--                <value>%logger</value>-->
<!--            </property>-->
<!--            <property>-->
<!--                <name>env</name>-->
<!--                <value>${active}</value>-->
<!--            </property>-->
<!--        </properties>-->
<!--        <headers>-->
<!--            <header>-->
<!--                <name>Content-Type</name>-->
<!--                <value>application/json</value>-->
<!--            </header>-->
<!--        </headers>-->
<!--    </appender>-->


    <!--myibatis log configure -->
    <logger name="com.apache.ibatis" level="DEBUG"/>
    <logger name="java.sql.Connection" level="DEBUG"/>
    <logger name="java.sql.Statement" level="DEBUG"/>
    <logger name="java.sql.PreparedStatement" level="DEBUG"/>

    <!--本地环境:打印控制台-->
    <springProfile name="local">
        <root level="${logLevel}">
            <appender-ref ref="consoleAppender" />
            <appender-ref ref="fileAppender"  />
            <appender-ref ref="errorAppender" />
<!--            <appender-ref ref="elasticAppender" />-->
        </root>
    </springProfile>

    <!--开发环境:输出到文件-->
    <springProfile name="dev">
        <!-- root日志以INFO级别输出，指定日志信息输出到哪个地方-->
        <root level="${logLevel}">
            <appender-ref ref="consoleAppender" />
            <appender-ref ref="fileAppender" />
            <appender-ref ref="errorAppender" />
<!--            <appender-ref ref="elasticAppender" />-->
        </root>
    </springProfile>

    <!--测试环境:输出到文件-->
    <springProfile name="test">
        <!-- root日志以INFO级别输出，指定日志信息输出到哪个地方-->
        <root level="${logLevel}">
            <appender-ref ref="consoleAppender" />
            <appender-ref ref="fileAppender" />
            <appender-ref ref="errorAppender" />
<!--            <appender-ref ref="elasticAppender" />-->
        </root>
    </springProfile>

</configuration>
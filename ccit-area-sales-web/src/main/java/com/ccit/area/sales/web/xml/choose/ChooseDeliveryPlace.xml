<?xml version="1.0" encoding="UTF-8"?>
<page id="chooseDeliveryPlace" version="1.0">
	<window id="deliveryPlaceWin" title="选择提货地档案" autoScroll="true" width="800" height="600" resize="false" maximizable="true">
		<gridpanel id="deliveryPlace" url="/webapi/archives/deliveryPlace/page/list" mode="single">
			<column label="提货地编码" prop="deliveryPlaceNo" xtype="textfield" />
			<column label="提货地名称" prop="deliveryPlaceName" xtype="textfield" />
			<column label="提货地分类编码" prop="deliveryPlaceClassNo" xtype="textfield" />
			<column label="提货地分类名称" prop="deliveryPlaceClassName" xtype="textfield" />
		</gridpanel>
		<button id="deliveryPlaceVerify" text="确定" iconCls="commitButton" handler="CrrHandler" callback="confirm" />
	</window>
	<script id="script"><![CDATA[
		// 触发事件
		function CrrHandler(t) {
			// 确定事件
			if (t.id == "deliveryPlaceVerify") {
				var records = Ext.getCmp("deliveryPlace").getSelectionModel().getSelection();
				if (records.length != 1) {
					Ext.MessageBox.alert("提示", "请选择一行进行确认!");
					return false;
				}
				eval(t.callback + "(records[0].data)");
				deliveryPlaceWin.close();
			}
		}
	]]></script>
</page>
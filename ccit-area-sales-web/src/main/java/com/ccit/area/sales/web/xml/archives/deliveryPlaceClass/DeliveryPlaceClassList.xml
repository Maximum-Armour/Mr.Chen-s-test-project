<?xml version="1.0" encoding="UTF-8"?>
<page id="deliveryPlaceClassList" version="1.0">
	<panel id="entrance" autoScroll="true">
		<gridpanel id="deliveryPlaceClassList" title="提货地分类" url="/webapi/archives/deliveryPlaceClass/page/list">
			<column label="主键ID" prop="id" xtype="numberfield" hidden="true" />
			<column label="提货地分类编码" prop="deliveryPlaceClassNo" xtype="textfield" renderer="linkurl" route="deliveryPlaceClassForm" />
			<column label="提货地分类名称" prop="deliveryPlaceClassName" xtype="textfield" />
			<column label="提货地分类排序" prop="deliveryPlaceClassOrders" xtype="numberfield" />
			<column label="状态" prop="status" xtype="combo" comboKey="status" />
			<column label="组织编码" prop="orgNo" xtype="textfield" hidden="true" />
			<column label="组织名称" prop="orgName" xtype="textfield" />
			<column label="部门编码" prop="deptNo" xtype="textfield"  hidden="true" />
			<column label="部门名称" prop="deptName" xtype="textfield" />
			<toolbar id="deliveryPlaceClassListTb">
				<button id="add" text="新增" iconCls="addButton" handler="CrrHandler" />
				<button id="edit" text="修改" iconCls="editButton" handler="CrrHandler" />
				<button id="delete" text="删除" iconCls="deleteButton" handler="CrrHandler" />
			</toolbar>
		</gridpanel>
	</panel>
	<script id="script"><![CDATA[
		// 触发事件
		function CrrHandler(t) {
			// 新增事件
			if (t.id == "add") {
				var data = {prefixTitle: "新增"};
				handleRoute("deliveryPlaceClassForm", data);
			}
			// 修改事件
			if (t.id == "edit") {
				var records = Ext.getCmp("deliveryPlaceClassList").getSelectionModel().getSelection();
				if (records.length != 1) {
					Ext.MessageBox.alert("提示", "请选择一行进行修改!");
					return false;
				}
				var data = {id: records[0].id, prefixTitle: "修改"};
				handleRoute("deliveryPlaceClassForm", data);
			}
			// 删除事件
			if (t.id == "delete") {
				var records = Ext.getCmp("deliveryPlaceClassList").getSelectionModel().getSelection();
				if (records.length < 1) {
					Ext.MessageBox.alert("提示", "请选择一行进行删除!");
					return false;
				}
				handleDelConfirm("/webapi/archives/deliveryPlaceClass/delete", records, Ext.getCmp("deliveryPlaceClassList"));
			}
		}
	]]></script>
</page>
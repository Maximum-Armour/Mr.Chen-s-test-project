<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccit.area.sales.dao.mapper.customerprofile.AccountInfomationMapper">

    <select id="selectPageList" resultType="com.ccit.area.sales.dao.vo.customer.TAccountInfomationVO">
        SELECT
        id,
        organization_name,
        login_account,
        phone_number,
        enterprise_email,
        status,
        resource_locking_status,
        created_time
        FROM t_account_infomation
        <where>
            is_deleted = #{param.is_deleted}
            <if test="param.organizationName != null and param.organizationName != ''">
                AND organization_name LIKE CONCAT('%', #{param.organizationName}, '%')
            </if>
            <if test="param.loginAccount != null and param.loginAccount != ''">
                AND login_account LIKE CONCAT('%', #{param.loginAccount}, '%')
            </if>
            <if test="param.phoneNumber != null and param.phoneNumber != ''">
                AND phone_number LIKE CONCAT('%', #{param.phoneNumber}, '%')
            </if>
            <if test="param.enterpriseEmail != null and param.enterpriseEmail != ''">
                AND enterprise_email LIKE CONCAT('%', #{param.enterpriseEmail}, '%')
            </if>
            <if test="param.status != null and param.status != ''">
                AND status LIKE CONCAT('%', #{param.status}, '%')
            </if>
            <if test="param.resourceLockingStatus != null and param.resourceLockingStatus != ''">
                AND resource_locking_status LIKE CONCAT('%', #{param.resourceLockingStatus}, '%')
            </if>
            <if test="param.createdTime != null and param.createdTime != ''">
                AND created_time LIKE CONCAT('%',#{param.createdTime}, '%')
            </if>
        </where>
    </select>
</mapper>

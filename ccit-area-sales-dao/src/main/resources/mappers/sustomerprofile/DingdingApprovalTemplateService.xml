<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ccit.area.sales.dao.mapper.customerprofile.CarTransportAddressesMapper">

    <select id="selectPageList" resultType="com.ccit.area.sales.dao.vo.customer.TCarTransportAddresseVO">
        SELECT
        id,
        customer_name,
        receiver_person,
        phone_number,
        transport_method,
        province,
        city,
        district,
        address,
        status
        FROM
        t_car_transport_addresses
        <where>
            is_deleted = #{param.is_deleted}
            <if test="param.customerName != null and param.customerName != ''">
                AND customer_name LIKE CONCAT('%', #{param.customerName}, '%')
            </if>
            <if test="param.receiverPerson != null and param.receiverPerson != ''">
                AND receiver_person LIKE CONCAT('%', #{param.receiverPerson}, '%')
            </if>
            <if test="param.phoneNumber != null and param.phoneNumber != ''">
                AND phone_number LIKE CONCAT('%', #{param.phoneNumber}, '%')
            </if>
            <if test="param.transportMethod != null and param.transportMethod != ''">
                AND transport_method LIKE CONCAT('%', #{param.transportMethod}, '%')
            </if>
            <if test="param.province != null and param.province != ''">
                AND province LIKE CONCAT('%',#{param.province}, '%')
            </if>
            <if test="param.city != null and param.city != ''">
                AND city LIKE CONCAT('%', #{param.city}, '%')
            </if>
            <if test="param.district != null and param.district != ''">
                AND district LIKE CONCAT('%', #{param.district}, '%')
            </if>
            <if test="param.address != null and param.address != ''">
                AND address LIKE CONCAT('%', #{param.address}, '%')
            </if>
            <if test="param.status != null and param.status != ''">
                AND status LIKE CONCAT('%',#{param.status}, '%')
            </if>
        </where>
    </select>
</mapper>
